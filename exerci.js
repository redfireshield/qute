// Initializing an object with a property named 'colfooters' as an empty array
let tableConfig = {
    colfooters: [],
    // Other properties can be added here
};

// Example of adding data to 'colfooters'
tableConfig.colfooters.push('Footer 1');
tableConfig.colfooters.push('Footer 2');

console.log(tableConfig);
